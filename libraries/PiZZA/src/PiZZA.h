/*
 * PiZZA — starter examples library for the PiZZA board
 * (Arduino on Zephyr, Raspberry Pi Zero 2 W).
 *
 * The examples here (Blink, HelloSerial) use only the standard Arduino
 * API, so you don't need to include this header. It exists so the
 * examples appear under "Examples for PiZZA" in the IDE.
 *
 * Notes for this board:
 *   - Serial is the mini-UART on GPIO14/15 @ 115200 (not USB CDC).
 *   - LED_BUILTIN is the on-board green ACT LED (BCM GPIO 29).
 *   - Upload copies the sketch to the SD card (mount it on your host).
 */
#pragma once
